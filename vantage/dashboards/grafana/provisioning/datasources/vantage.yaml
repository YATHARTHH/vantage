apiVersion: 1

datasources:
  - name: Vantage API
    type: yesoreyeram-infinity-datasource
    access: proxy
    isDefault: true
    jsonData:
      baseURL: "http://vantage:8000/api/v1/query"
      auth_type: "headers"
      httpHeaderName1: "X-API-Key"
    secureJsonData:
      httpHeaderValue1: "dev-local-key"
